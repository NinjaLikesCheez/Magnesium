skip_docs

default_platform(:ios)

platform :ios do
  desc "Runs tests"
  lane :test do
    scan(
      scheme: "Preferences",
      derived_data_path: "build"
    )
    scan(
      scheme: "Magnesium",
      derived_data_path: "build"
    )
  end
end
